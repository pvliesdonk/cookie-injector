[project]
name = "cookie-injector-health"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "structlog>=24.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.3.0",
]
